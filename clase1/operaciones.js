//EXPORT dice que esta listo para utilizarse desde otro archivo
export const sumar = (numeroA, numeroB) => numeroA + numeroB;

export const restar = (numeroA, numeroB) => numeroA - numeroB;

//Para evitar utilizar la palabra export en cada comando, se pued eescribir al final:

//export {sumar, restar};